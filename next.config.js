/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  // ...resto de tu config (si tenés otras opciones, mantenelas acá)
}

module.exports = nextConfig

